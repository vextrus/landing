# Hoist all (incl. transitive @types like geojson / topojson-specification) to the
# node_modules root so TS type-only imports resolve under strict pnpm — mirrors the
# source monorepo so the build matches what was verified there. Vercel respects this.
shamefully-hoist=true
node-linker=hoisted
